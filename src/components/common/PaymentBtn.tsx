import { Button } from '../ui/button';

const PaymentBtn = ({ className = '' }: { className: string }) => {
  return <Button className={`${className}`}>PaymentBtn</Button>;
};

export default PaymentBtn;
